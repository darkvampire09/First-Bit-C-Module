#include<stdio.h>
int main()
{
int start=0,end=5,no,add;
    while(start<end){
      no=no+start;//no =gbb ,no+0=0 , next iteration no=gb , no+1=1 so no be 1 and so on 
        start++;//increrase start from 1 to 5

        printf("%d\n",start);
        add=start+add;
        
    }
    printf("%d",add);
    return 0;
}